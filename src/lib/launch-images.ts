/** High quality for photos/posters that go through next/image optimization. */
export const LAUNCH_IMAGE_QUALITY = 90;

/** Use `unoptimized` on UI mockup PNGs to preserve crisp text and edges. */
export const LAUNCH_MOCKUP_UNOPTIMIZED = true;
